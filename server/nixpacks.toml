# Force Node.js detection with Python support
[phases.setup]
nixPkgs = ["nodejs", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm ci || npm install", "pip install requests python-dotenv google-generativeai"]

[start]
cmd = "node server.js"
