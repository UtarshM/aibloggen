# Force Node.js detection - Updated
[phases.setup]
nixPkgs = ["nodejs", "python311"]

[phases.install]
cmds = ["npm ci || npm install"]

[phases.build]
cmds = ["pip install -r requirements.txt || echo 'Python deps skipped'"]

[start]
cmd = "node server.js"
