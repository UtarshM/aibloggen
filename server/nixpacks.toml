# Force Node.js detection
[phases.setup]
nixPkgs = ["nodejs_18", "npm", "python311"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["pip install -r requirements.txt || true"]

[start]
cmd = "node server.js"
